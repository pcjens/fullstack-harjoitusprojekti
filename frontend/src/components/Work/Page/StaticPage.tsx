import { Work } from "..";

export const WorkStaticPage = ({ portfolioSlug, work }: { portfolioSlug: string, work: Work }) => {
    return (
        <>TODO: show {portfolioSlug}/{work.slug}</>
    );
};
